import styles from "./Header.module.css";

const Form = () => {
    return (
        <header className={styles.header}>
            <h1>Olá, vamos calcular seu IMC</h1>
        </header>
    );
};

export default Form;
