import "./header.css";

const Form = () => {
    return (
        <header className="container header">
            <h1>Olá, vamos calcular seu IMC</h1>
        </header>
    );
};

export default Form;
